- [2026-04-11] The system target for llm_wiki_prompt_packet is the combined Kade-HQ plus G-Stack harness, using Byterover for durable memory, GitVizz for graph and web surfaces, and pk-qmd for Gemini-backed embeddings plus hybrid RAG and agentic search.
- [2026-04-11] The richer repo-owned harness runtime is expected under `deps/pk-skills1`; packet-owned `skills/home/g-kade` and `skills/home/gstack` remain launcher wrappers, not the richer runtime itself.
- [2026-04-11] Local Docker intentionally exposes one loopback-only host gateway at `127.0.0.1:8181`: `/mcp` for `pk-qmd`, `/graph/*` for GitVizz backend routes, and `/memory/*` for a narrow BRV adapter.
- [2026-04-11] Local Docker gateway routes are intentionally auth-free only because they bind to loopback. Hosted or tunnelled access should add auth and must not expose raw BRV credentials, Mongo, or Phoenix directly.
- [2026-04-11] `installers/install_g_kade_workspace.py` should create `~/.kade/HUMAN.md` from the packaged `deps/pk-skills1/kade-headquarters/HUMAN.md` profile when missing, upgrade only the exact legacy stub, and preserve any real existing Layer 1 profile.
- [2026-04-12] The official durable Obsidian memory base is `C:\dev\Desktop-Projects\Helpful-Docs-Prompts\VAULTS-OBSIDIAN\Kade-HQ` with vault name `kade-hq`, vault id `fd8411f00d3a9d21`, and canonical system note `llm_wiki_prompt_packet System Map.md`.
- [2026-04-12] Packet-owned launcher installs must include `kade-hq`, `g-kade`, and `gstack`; `g-kade` is only the unifier skill and must not be treated as the KADE System itself.
- [2026-04-12] Install flows should preserve the explicit choice between local project bootstrap and global managed-tool bootstrap through `-g` or `--global-install`.
- [2026-04-12] Shared setup and health logic now lives in `scripts/llm_wiki_memory_runtime.py`; Bash, PowerShell, and CMD entrypoints are thin wrappers, `pk-qmd` is pinned to commit `ef26cb62bb8132bc3a851b23f450af8e382e4c4e`, and the gateway now refuses non-loopback host exposure without auth unless an explicit unsafe override is set.
- [2026-04-12] The local skill-learning plane now includes an EvoSkill-style auto-improvement loop in `support/scripts/llm_wiki_skill_mcp.py` with persisted proposals, surrogate reviews, evolution runs, and a top-k frontier snapshot under `.llm-wiki/skill-pipeline/`; installer bootstrap must seed those paths and config keys for new workspaces.
- [2026-04-13] `support/scripts/llm_wiki_skill_mcp.py` now separates verifier modes inside `skill_evolve`: strong objective runs still use explicit oracle or benchmark-style verdicts when supplied, while subjective tasks can use an adapted VMR-style pairwise verifier that stores randomized A/B options, a cross-agent plain-text judge prompt, rubric criteria, and a binary candidate-vs-baseline outcome instead of relying only on heuristic surrogate status.
- [2026-04-21] Keep packet-owned wrapper guidance abstract and simple while preserving the Kade homage. Prefer human layer first, then project/session layer, then harness/memory mechanism details.
- [2026-04-21] The skill-learning plane now also carries WorldDB-style write-time reconciliation hints via `canonical_keys` so similar skills merge/update more reliably instead of accumulating flat duplicates.
- [2026-04-12] The skill-learning plane now also includes `support/scripts/llm_wiki_failure_collector.py`, a local-first repeated-failure collector that stores failure events, cluster summaries, and benchmark promotion records under `.llm-wiki/skill-pipeline/failures/`, then promotes eligible clusters into `skill_evolve` using configurable thresholds instead of promoting every raw error.
- [2026-04-12] Automatic Claude-side failure emission is now wired through `support/scripts/llm_wiki_failure_hook.py`; `support/scripts/llm_wiki_memory_runtime.py` installs that hook into project-local `.claude/settings.local.json` so `PostToolUseFailure` and `StopFailure` events feed the repeated-failure collector without committing machine-specific hook commands into the repo.
- [2026-04-12] Cross-agent CLI failure capture now also uses `support/scripts/llm_wiki_agent_failure_capture.py` plus `run_llm_wiki_agent.ps1`, `.sh`, and `.cmd` for Claude Code, Codex, Factory Droid, and `pi`; Claude keeps native failure hooks, while Codex, Droid, and `pi` use the shared wrapper because they do not expose an equivalent packet-usable failure-event hook surface. The PowerShell wrapper now has a machine-safe `-ArgumentJson` path for forwarded agent flags, while manual shell and cmd launchers keep their normal pass-through surfaces.
- [2026-04-13] The repo now has a reusable Gym-Anything Pokemon self-test surface at `support/scripts/pokemon_benchmark_adapter.py` plus `support/scripts/run_pokemon_benchmark.ps1`. It opens a live benchmark session, can run a deterministic headless smoke flow or a wrapper-driven framework flow through `run_llm_wiki_agent.ps1`, emits normalized result packets under `.artifacts/pokemon-benchmark/runs/`, and uses a narrow `session-write-report` helper so container-side report creation stays deterministic instead of depending on shell quoting.
- [2026-04-13] Packet-owned benchmark launch now also includes a reusable `skills/home/pokemon-benchmark` skill. Bootstrap installers copy that skill into home roots and repo-local `.agents/.codex/.claude` skill surfaces, and they now copy `scripts/pokemon_benchmark_adapter.py` plus `scripts/run_pokemon_benchmark.ps1` into neutral repos so the installed skill can run smoke and framework evaluations without depending on the source packet checkout.
- [2026-04-13] Pi home-skill install now targets `C:\Users\prest\.pi\agent\skills`, not `C:\Users\prest\.pi\agents\skills`. That Pi surface carries the packet launcher trio (`kade-hq`, `g-kade`, `gstack`) plus `pokemon-benchmark`, and the mistaken `.pi\agents\skills\pokemon-benchmark` install was removed after the benchmark prompt/skill text was aligned to frame `pokemon-benchmark` as the benchmark-side coordinator rather than the evaluated harness by itself.
- [2026-04-13] Project activation now has a dedicated PowerShell wrapper at `support/scripts/activate_llm_wiki_project.ps1`. The intended quickstart is: point that wrapper at a target repo, let it call `install_g_kade_workspace.py`, install packet home skills including `C:\Users\prest\.pi\agent\skills`, and run setup/check so `pk-qmd`, `brv`, and configured `GitVizz` endpoints are activated for that project instead of leaving activation as a manual installer argument puzzle.
